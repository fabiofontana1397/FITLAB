# Copy to supabase/.env.local (gitignored) and fill in real values.
# SUPABASE_URL / SUPABASE_ANON_KEY / SUPABASE_SERVICE_ROLE_KEY are already
# auto-injected by the local CLI/edge runtime — do NOT set them here.
ANTHROPIC_API_KEY=
VOYAGE_API_KEY=
